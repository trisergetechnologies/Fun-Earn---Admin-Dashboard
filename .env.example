# Copy to .env for local dashboard development

# Backend API (must match backend PORT and /api/v1 prefix)
NEXT_PUBLIC_BASE_URL=http://localhost:5000/api/v1
NODE_ENV=development

# Production:
# NEXT_PUBLIC_BASE_URL=https://amp-api.mpdreams.in/api/v1
# NODE_ENV=production
